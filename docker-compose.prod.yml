networks:
  traefik:
    external: true
  monitoring:
    external: true
  enhanced-git-prod:
    external: true

name: enhanced-git-prod
services:
  prod-front:
    container_name: prod-front
    build:
      context: .
      dockerfile: Dockerfile
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.prod-front.rule=Host(`enhanced-git.com`)"
      - "traefik.http.routers.prod-front.entrypoints=websecure"
      - "traefik.http.routers.prod-front.tls=true"
      - "traefik.http.routers.prod-front.tls.certresolver=cloudflare"
      - "traefik.http.routers.prod-front.rule=Host(`enhanced-git.com`)"
      - "traefik.http.routers.prod-front.entrypoints=web"
      - "traefik.http.routers.prod-front.middlewares=redirect-to-https"
      - "traefik.http.middlewares.redirect-to-https.redirectscheme.scheme=https"
      - "traefik.docker.network=traefik"
    logging:
      driver: loki
      options:
        loki-url: "http://localhost:3100/loki/api/v1/push"
        loki-batch-size: "400"
        loki-retries: "5"
        loki-external-labels: "app=prod-front"

    networks:
      - traefik
      - enhanced-git-prod
      - monitoring
